s = open("24-actual/24.69902.txt").read()
s = s.replace('DE', 'D E').split()

# maxi = (len(''.join(s[i:i + 241])) for i in range(len(s)))

# print(max(maxi))

# Объяснение:
# 1. Читаем файл в строку s
# 2. Заменяем каждую пару 'DE' на 'D E' (с пробелом между буквами)
# 3. Разбиваем строку по пробелам - получаем список участков между парами DE
# 4. Берём 241 элемент подряд (между ними будет 240 пробелов = 240 пар DE)
# 5. Склеиваем эти элементы обратно в строку и считаем длину
# 6. Перебираем все возможные начальные позиции и находим максимум
# 7. Выводим результат
